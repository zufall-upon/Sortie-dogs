# v0.10 goal/proposal recovery — 2026-09-23

## Candidate and preserved state

- Base: `b2e954303fcd1cd49c14a82060640fcb0f99739f` (`0.10.22`).
- Latest pre-integration candidate tarball: SHA-256 `43c02886b7a956ad776137a11fe2d325c497bda0b7c1024f3d77cad0ce983be6` (`0.10.22`). The first isolated CLI attempts used the earlier `0a7af9f0e9031fc2839027856f3b676242e5a72bdba030f2864df1a79c804332` package; the first same-session submission/revision/approval attempt used `f6017edf6e509d99d33cd70a7cca51aaa08df7c63269621cdd58235455e14abc`. The latest package adds an active-goal epoch/fingerprint recheck before orphan settlement.
- The original project state was read-only throughout this investigation. Real-state replays copied the exact proposal, cancelled operator and goal ledger into separate temporary Git roots; the copies, raw host history and CLI transcripts are outside Git under the approved OpenCode temporary directory.
- Root `ses_f3cab09b5ffdVTVIGWiOfxyvPV`: submitted `proposal-a2ff9b4b06ac148e19a1bd42` (revision 1, hash `e95521afb89002b5f23e5593b9ef949e17d3a9b1c2e3218036932292b6ff0b09`), R1–R16, 180 reads and 16 submissions. An exact-identity, same-goal cumulative increase to 228/22 left 48/6. The proposal identity, ordered requirements, goal binding and spend were identical after the transition. Because the old write scope excludes observed host files, a separate root-authorized scope reopening archived the submitted proposal and issued a **new read-only investigation reference**, retaining all spend. It did not approve the old plan, dispatch a new Task or claim acceptance.
- Vibe-Dev-Mate root `ses_f38b05f66ffdLGfLtbhHm5n7TA`: approved proposal bound to an earlier goal; the currently active goal has a different ID and one orphaned reservation. The orphan belongs to a nested worker Task left `running` in native history when its parent delegate was aborted. The failed delegate's own Task ID cannot be used to settle that worker reservation.

## Exact-history red/green

- Copied the real goal ledger and the three native V2 sessions (root, delegate and worker) into an isolated plugin replay. Old installed plugin: orphan count 1, consumed 0. Candidate: root-only recovery checked aborted delegate, exact run/Task reference, reservation fingerprint, owned direct session lineage, worker task ID and last activity before parent termination. The replay's interrupt API was **stubbed** with an idle acknowledgment (`interrupted: false`) for the two historical owned sessions; no live OpenCode process was interrupted. After simulated recovery: orphan count 0, consumed 1, satisfied criteria 0; no acceptance receipt.
- Starting with the archived approved proposal and cancelled replacement run, a root revision on the currently active goal required the original parent run's dispatch **and** settlement in the old goal ledger. This retained the ordered R1–R7, archived the old contract, carried 43 reads/3 submissions and returned a new investigation reference. A missing parent proof or stale identity is denied. The old run was neither accepted nor revived.
- Unit coverage includes stale hashes, other actors/goals, exhausted limits, undeclared write paths, missing native lineage, failed interrupt confirmation, cold restart and duplicate settlement. A V2 short-reference regression also covers a terminal Task that directly owns its reservation.

## Gates and limits

- `npm run build`, `npm run test:full` (WSL snapshot, exit 0, source SHA-256 `d26871d96dc16167b5715dadd5783de5321fda1ea367f4d0ce24836b7ec8b47e`, `_testenv/wsl-1790149092417-42576/`), `npm run test:windows` (6/6): PASS for the candidate source.
- The isolated `opencode run --standalone` CLI initially selected `opencode/ling-3.0-flash-fin-free` for the proposal child; the provider returned HTTP 403 (`OpenCode's free tier can only be used from within OpenCode`). A separate isolated config pinned V2 project-agent models and disabled its free-tier fallback before the test began. The real CLI then submitted a proposal (4 reads, 1 submission), increased that same submitted proposal's cumulative limit from 10/1 to 10/2, repaired its R1 defect by an exact root revision (4 reads, 2 submissions), and explicitly approved revision 2 in the same native session/goal. The next native worker Task stopped before work with `Model unavailable: openai/gpt-6-luna-fast`; it consumed the second goal unit as a process defect. No validation or acceptance was claimed for that earlier scenario.
- The **latest package** ran a fresh four-turn native CLI scenario, with the available `anthropic/claude-opus-5-5` pinned for root, proposal child and worker before turn one. Root `ses_f32c027b4ffe0utsBYkmVLOFdB` retained one goal (`sha256:7e83d03318b3e0ec793bfd9c6c456c07e6bb34159b875e61d066bddc323889aa`) and ordered R1–R3. The child submitted an intentionally deficient proposal (4/10 reads, 1/1 submission, one goal unit). Root raised only the submitted proposal's cumulative submission ceiling to 2, corrected its known R1 coverage/objective by a pinned revision (still 4 reads, now 2/2 submissions), compared the corrected packet, and explicitly approved it. One worker then wrote only `output.json` (`{"answer":42}\n`), ran `node check.mjs` with native exit 0, and root completed the run only after checking the result. Independent post-run `node check.mjs` exited 0; hashes of all five protected fixture files matched their starting copies. The durable goal receipt and operator receipt were `succeeded`, consumed units 2/2, reservations 0. The scenario spent four CLI turns, within its eight-turn limit; no proposal child or worker was redispatched.
- V2 return-report audit on the **native SQLite message records**: one terminal assistant response contains one `<details>` report panel; its suffix exactly equals the `complete_operator` tool result's `return_report` (SHA-256 `8833868b0799f82448f324c6fb430d731a3d1e4bb6c3a34ec609a0bfdf7f5e9a`). Root, proposal child and worker are the only three native sessions; root inbox has zero items. Windows PowerShell stdout redirection damaged non-ASCII glyphs in the JSONL transcript, so native stored UTF-8 message and tool result are the authoritative display-text evidence. This check does not establish web UI display/reopen behavior or complete cost telemetry.
- The exact-history replay is not proof that the live host can interrupt those historical sessions or that the current user root has executed this transition. No actual MK2-02 live screen, Visual Go, independently accepted root report, three-case campaign completion, or user-only acceptance has been established by this replay.
