{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["sortie-dogs/plugin"],
  "command": {
    "sortie": {
      "description": "Run the Sortie workflow",
      "agent": "build",
      "template": "Run the Sortie workflow. $ARGUMENTS"
    }
  }
}
